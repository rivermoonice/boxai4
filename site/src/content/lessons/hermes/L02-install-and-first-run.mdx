---
title: "How to Install Hermes Agent for Beginners (2026): Mac and VPS, Step-by-Step"
module: hermes
slug: l02-install-and-first-run
order: 2
updatedDate: 2026-06-30
updatedRaw: "June 2026"
summary: "Install Hermes on Mac or VPS in under twenty minutes. Pick a model, paste an API key, verify with `hermes doctor`."
description: "Mac install is ten minutes. VPS install is twenty minutes. Pick a cheap model first, set a $20/month budget cap, run `hermes doctor` to verify."
readingMinutes: 9
accent: "#7c5cff"
---

**Last tested and updated:** June 2026

import TerminalScreenshot from '../../../components/lesson/TerminalScreenshot.astro';
import VidCard from '../../../components/lesson/VidCard.astro';
import Callout from '../../../components/lesson/Callout.astro';
import { Picture } from 'astro:assets';

import hermesSetupHelp from '../../../assets/lessons/hermes/L02/hermes-setup-help.png';
import hermesDoctor from '../../../assets/lessons/hermes/L02/hermes-doctor.png';
import hermesSkills from '../../../assets/lessons/hermes/L02/hermes-skills.png';

Hermes installs in one curl-pipe-bash on Mac or VPS; pick Mac first to learn.

## Before you install

Install Xcode Command Line Tools first. Skip this and the installer fails with "no developer tools were found."

Open Terminal (Finder → Applications → Utilities → Terminal) and run:

```bash
xcode-select --install
```

Click **Install**. The first run downloads 500 MB and takes five to ten minutes.

<Callout type="gotcha" title="Don't skip Xcode CLT">
The installer gives a misleading "command not found" error. The cause is always the missing CLT, never the installer itself.
</Callout>

You also need a model provider account. Hermes is the harness — it does not bundle model access. Bring your own key (BYOK = your provider's API key). Supported providers are Anthropic, OpenAI, DeepSeek, Kimi, MiniMax, and Qwen. The Mac path needs macOS 13 or newer.

## Two paths: Mac vs VPS

Pick Mac for learning. Pick VPS for 24/7 cron jobs. A $3/month VPS keeps running when your laptop lid is closed.

<Picture src="/lessons/hermes/L02/mac-vs-vps-decision.svg" alt="Decision diagram: Mac path runs locally for free but only while your laptop is open. VPS path costs $2–5/month and runs 24/7." width={880} height={480} formats={["avif", "webp"]} />

**Mac (local).** Hermes runs on your laptop. Free. Fastest iteration. Downside: only runs while your laptop is awake.

**VPS (rented Linux server).** Hermes runs on a small rented Linux box. $2–$5/month at Hetzner, DigitalOcean, Vultr, or Linode. Runs 24/7. Downside: you must learn SSH.

<Callout type="tip" title="Ron's rule">
"If you've never used a terminal, pick VPS. You learn one transferable skill — SSH — that you'll use forever." — Ron, source video `UbK2kXygPUY`
</Callout>

| Situation | Path |
|---|---|
| Brand new to terminals or cheapest test drive | Mac |
| Want 7am cron while you sleep | VPS |
| On a Windows PC | VPS |
| Plan to wire Telegram or Discord bots | VPS |

### The Mac path

After `xcode-select --install` finishes:

```bash
curl -fsSL https://hermes-agent.newsresearch.com/install.sh | bash
hermes
```

Pick **quick setup** in the wizard. Skip Telegram and Discord on first install. [L05](/lessons/hermes/l05-mcp-catalog-and-integrations/) wires those properly.

### The VPS path

The VPS path = SSH (Secure Shell, an encrypted terminal into a remote box) plus the same installer as Mac.

<Picture src="/lessons/hermes/L02/ssh-vps-flow.svg" alt="Four-step flow: laptop SSHes into VPS, VPS runs installer, wizard writes API key to ~/.hermes/config.yaml, you chat with Hermes over SSH." width={900} height={280} formats={["avif", "webp"]} />

1. **Rent a server.** Ubuntu 22.04 or 24.04, 2 GB RAM, $2–$5/month.
2. **SSH in.** `ssh ubuntu@<your-server-ip>`. Type `yes` to trust the host fingerprint. Paste the password — the cursor does not move. That is normal.
3. **Install deps.** `sudo apt update && sudo apt install -y git curl`.
4. **Run installer.** Paste the one-line command. When asked about importing from OpenClaw, answer **No** — even if you used OpenClaw before.

<Callout type="gotcha" title="Don't import from OpenClaw">
The source video tested the import. Cron jobs did not migrate cleanly. A fresh install is more reliable than a migrated one.
</Callout>

## Pick your model

Start with Kimi K2.6 or DeepSeek V4 Pro. Upgrade to Claude Sonnet only when you can see the loop working and need the quality.

Picking the right model decides whether you spend $5/month or $50/month. The wizard asks for a provider and an API key. You can swap providers later by re-running `hermes setup`.

| Provider | Pick when… |
|---|---|
| Kimi K2.6 (Moonshot) | Cost matters most; daily use, non-English |
| Claude Sonnet 4.8 (Anthropic) | Reliable quality, broad capability |
| Claude Opus 4.8 (Anthropic) | Long-context or high-stakes deliverables |
| DeepSeek V4 Pro | Cost-efficient, broad capability |
| OpenAI GPT-5.x | Solid general-purpose |
| MiniMax M3 | Cost-efficient execution |

At 50–100 messages per day on the cheap tier, expect **$5–$10/month**. Claude Opus runs 4–5× that.

**Set a $20/month hard cap** in the provider's billing dashboard on day one. API keys are passwords. Paste them only into the wizard.

<Callout type="bottom-line" title="Ron's bottom line: pick cheap, upgrade later">
Start with Kimi K2.6 or DeepSeek V4 Pro. Upgrade to Claude Sonnet only for queries that need it. Pay-for-quality is fine. Pay-for-everything is how you overspend.
</Callout>

## Verify it works

Run these three commands. All three must return without error.

```bash
hermes doctor          # checks Python, SSL, dependencies
hermes status          # shows config, API keys, version
hermes skills list     # enumerates available skills
```

A healthy doctor looks like this.

<TerminalScreenshot
  image={hermesDoctor}
  title="hermes doctor"
  alt="Output of hermes doctor showing green checks across security, Python, SSL, and dependencies"
  caption="Everything green = healthy install. Orange Telegram/Discord warnings resolve in L05."
/>

`hermes setup --help` lists subsections you can re-run individually — useful for swapping providers later.

<TerminalScreenshot
  image={hermesSetupHelp}
  title="hermes setup --help"
  alt="Output of hermes setup --help showing six sub-sections: model, tts, terminal, gateway, tools, agent"
  caption="Re-run `hermes setup model` to swap providers — no need to redo the whole wizard."
/>

`hermes skills list` shows what Hermes can invoke.

<TerminalScreenshot
  image={hermesSkills}
  title="hermes skills list"
  alt="Output of hermes skills list enumerating built-in skills with one-line descriptions"
  caption="Each row is a skill Hermes knows. L04 covers adding your own."
/>

The final smoke test: launch chat, type `hi`. If Hermes responds, the install is complete.

```bash
hermes
> hi
< Hello! How can I help today?
```

A one-liner that proves the same thing without interactive chat:

```bash
hermes doctor >/dev/null 2>&1 && echo "hermes install verified" || echo "hermes install FAILED"
```

If you see an error, the cause is usually a wrong API key. Re-run `hermes setup` to fix a bad key. On VPS, install missing deps with `sudo apt install -y curl git`.

### Watch the full install

<VidCard id="lHVk1Agz3_I" title="Hermes Agent Setup on Mac — full Mac walkthrough, Ron, April 2026" />
<VidCard id="UbK2kXygPUY" title="Hermes Agent Setup on VPS — full VPS walkthrough with SSH, Ron, April 2026" />

### Check your understanding

Quiz lives at `site/src/lessons/hermes/L02-install-and-first-run/quiz.json` — 6 questions, runnable by the lesson harness.

### What's next

- **Pick your daily interface.** → [L03 — Hermes TUI vs Desktop App](/lessons/hermes/l03-daily-driver-tui-vs-desktop/).
- **Lock down security.** → [L07 — How to Secure Your Hermes Agent](/lessons/hermes/l07-security-essentials/). Required within a week, especially on VPS.
- **Schedule a job.** → [L08 — Automation: cron, kanban, sub-agents](/lessons/hermes/l08-automation-cron-kanban-sub-agents/). That is the workflow you signed up for in [L01](/lessons/hermes/l01-what-is-hermes-agent/).

<Callout type="bottom-line">
**Ron's bottom line:** week 1 = Mac; week 2 = try VPS on a $3 box.
</Callout>
